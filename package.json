{
  "name": "mj-life-choices",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "迈克尔·杰克逊：人生选择 — 文字人生模拟游戏（原生 JS，模块化，可选 Vite 单文件构建）",
  "scripts": {
    "dev": "vite",
    "build": "node build_singlefile.cjs",
    "preview": "vite preview",
    "test": "node test/check_dup_events.cjs && node test/check_variant_windows.cjs && node test/check_dream_achievements.cjs && node test/en_smoke.cjs",
    "test:full": "node test/smoke.cjs && npm test"
  },
  "devDependencies": {
    "vite": "^5.4.10",
    "vite-plugin-singlefile": "^2.0.3"
  }
}
